<div class="round-title">
  <h3>Round {{model.id}}</h3>
  <p>{{model.score}}</p>
</div>
<div class="form-group">
  {{#each model.scores as |score|}}
    {{score.hole.number}}
    {{score.strokes}}<br>
  {{/each}}
</div>

